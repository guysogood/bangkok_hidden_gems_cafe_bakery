# --- Secrets: never commit these ---
.env
.env.*
!.env.example
*_key.json
secrets/

# --- Docker / Airflow runtime ---
logs/

# --- Virtual environment (only used for local, non-Docker script runs) ---
.venv/
venv/

# --- Python ---
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.ruff_cache/

# --- PySpark / Spark local runtime artifacts ---
spark-warehouse/
metastore_db/
derby.log
*.crc
_SUCCESS

# --- The GCS connector jar (large, download separately, don't commit) ---
gcs-connector-hadoop3-latest.jar
gcs-connector-*.jar

# --- Local data files (bronze/silver/gold outputs if ever written locally) ---
*.parquet
bronze_places_raw.json
bronze_*.json

# --- Jupyter ---
.ipynb_checkpoints/

# --- IDE / editor tooling ---
.claude/
CLAUDE.md
.vscode/settings.local.json
.vscode/*.local.json

# --- macOS ---
.DS_Store